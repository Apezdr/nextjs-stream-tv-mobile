diff --git a/node_modules/@react-native-cookies/cookies/android/build.gradle b/node_modules/@react-native-cookies/cookies/android/build.gradle
index 13c7e43..77905d9 100644
--- a/node_modules/@react-native-cookies/cookies/android/build.gradle
+++ b/node_modules/@react-native-cookies/cookies/android/build.gradle
@@ -31,7 +31,7 @@ buildscript {
     if (project == rootProject) {
         repositories {
             google()
-            jcenter()
+            mavenCentral()
         }
         dependencies {
             classpath 'com.android.tools.build:gradle:3.5.3'
@@ -42,6 +42,12 @@ buildscript {
 apply plugin: 'com.android.library'
 
 android {
+    // AGP 8 removed the fallback to the manifest's `package` attribute, so the
+    // namespace has to be declared here or evaluation fails.
+    def agpVersion = com.android.Version.ANDROID_GRADLE_PLUGIN_VERSION.tokenize('.')[0].toInteger()
+    if (agpVersion >= 7) {
+        namespace 'com.reactnativecommunity.cookies'
+    }
     compileSdkVersion safeExtGet('compileSdkVersion', DEFAULT_COMPILE_SDK_VERSION)
     buildToolsVersion safeExtGet('buildToolsVersion', DEFAULT_BUILD_TOOLS_VERSION)
     defaultConfig {
@@ -67,7 +73,7 @@ repositories {
         url "$rootDir/../node_modules/jsc-android/dist"
     }
     google()
-    jcenter()
+    mavenCentral()
 }
 
 dependencies {
